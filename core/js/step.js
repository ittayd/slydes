/**
 * A step is a transition in the presentation. Can be a slide or an effect within a slide (e.g., bullets showing on after the other)
 */
(function($){
	Slydes.Step = function(index, element, options) {
		return this;
	}
})(jQuery)